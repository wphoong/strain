<h1> Welcome to My Store</h1>
<%= link_to 'Edit Store?', edit_store_path(@stores), class: 'btn btn-success' %>
<%= link_to 'Delete Store?', store_path(@stores), method: :delete, data: { confirm: 'Are you sure you want to delete this store?'}, class: 'btn btn-danger' %>

<h1><%= @stores.storename %></h1>

<p><%= @stores.description %></p>

<p><%= @stores.location %></p>

<p><%= @stores.phonenumber %></p>


<h1>Products</h1>

<%= link_to 'Add a new Product', new_store_product_path(@stores), class: 'btn btn-info' %>
